<div class='accident-data'>
    <label>Local da última decolagem:</label>
    <input type="text" name='lastTakeOff' class='customInput' />

    <label >Local pretendido de pouso:</label>
    <input type="text" name='landing' class='customInput' />

    <label id='registerCategory'>Categoria de registro:</label>
    <select name='registerCategory' class='customInput' >
        <option selected disabled></option>
        <% registerCategoryModel.forEach((item) => { %>
        <option value=<%= item.id %> > <%= item.value %> </option>
        <% }) %>
    </select>

    <label id='operationType'>Tipo de operação:</label>
    <select name='operationType' class='customInput'>
        <option selected disabled></option>
        <% operationTypeModel.forEach((item) => { %>
        <option value=<%= item.id %> > <%= item.value %> </option>
        <% }) %>    
    </select>

    <label>Fase da operação:</label>
    <select id='operationPhase' name='operationPhase' class='customInput'>
        <option selected disabled></option>
        <% operationPhaseModel.forEach((item) => { %>
        <option value=<%= item.id %> > <%= item.value %> </option>
        <% }) %>    
    </select>

    <label>Danos à aeronave:</label>
    <select id='aircraftDamage' name='aircraftDamage' class='customInput'>
        <option selected disabled></option>
        <% aircraftDamageModel.forEach((item) => { %>
        <option value=<%= item.id %> > <%= item.value %> </option>
        <% }) %>
    </select>
</div>

<div class='accident-data'>
    <label>Tipo de aeronave:</label>
    <select id='aircraftType' name='aircraftType' class='customInput'>
        <option selected disabled></option>
        <% aircraftTypeModel.forEach((item) => { %>
        <option value=<%= item.id %> > <%= item.value %> </option>
        <% }) %>
    </select>

    <label>País de registro:</label>
    <input type="text" name='registryCountry' class='customInput' />

    <label>Ano de fabricação:</label>
    <input type="text" name='manufacturingYear' class='customInput' />

    <label>Fabricante:</label>
    <input type="text" name='manufacturer' class='customInput' />

    <label>Modelo:</label>
    <input type="text" name='model' class='customInput' />

    <div class='div-max-weight'>
        <label>Peso máximo de decolagem:</label>
        <div>
            <label for='kg'>Kg</label>
            <input type='radio' name='maxTakeOffWeightOption' value="1" id='kg' class='customRadio' />
            <label for='lbs'>Lbs</label>
            <input type='radio' name='maxTakeOffWeightOption' value="2" id='lbs' class='customRadio' />
            <input type="number" name='maxTakeOffWeight' class='customInput' />
        </div>
    </div>
</div>